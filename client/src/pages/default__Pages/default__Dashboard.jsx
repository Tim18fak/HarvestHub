/* import React from 'react'
import { cookie } from '../../../configs/default__configs/cookies'
import ClientRoute from '../../routes/client__Route/ClientRoute'
import FarmerRoute from '../../routes/farmer__Route/FarmerRoute'
const DefaultDashboard = () => {
    const isFarmer =  cookie.get('isFarmer')
    if(isFarmer === true) return <FarmerRoute/>
  return <ClientRoute/>
}

export default DefaultDashboard */
